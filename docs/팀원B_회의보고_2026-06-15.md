# 팀원 B 회의 보고 (2026-06-15)

> VR 인터랙션 / 투사체 담당

---

## ✅ 이번 주 완료한 것

1. **작업 환경 설정 완료**
   - `Test_B_Scene` 작업 씬 확인
   - Meta XR All-in-One SDK / XR Interaction Toolkit 패키지 설치 준비

2. **투사체 시스템 골격 작성 완료** (push 완료)
   - `Assets/Scripts/Projectiles/Projectile.cs` — 모든 투사체의 부모 클래스
     - 충돌 감지(`OnCollisionEnter`)
     - 자동 삭제 타이머(`lifeTime`)
     - 데미지 변수(`damage`) — A의 `WallBlock`에 전달할 인터페이스 준비
   - `Assets/Scripts/Projectiles/ExplosiveProjectile.cs` — 폭탄 투사체
     - `Physics.OverlapSphere` + `AddExplosionForce`로 주변 블록 날리는 로직
     - 폭발 VFX/SFX hook
     - 에디터 기즈모로 폭발 범위 시각화

3. **MoSCoW 표 기준 우선순위 정리**
   - Must: 기본 투사체 / 던지기 — **다음 주 시작**
   - Should: 폭탄, 인벤토리 벨트
   - Could: 유도 미사일, 가챠 연동

---

## 🚀 다음 주 계획

| 일정 | 내용 |
|------|------|
| 다음 주 전반 | `Test_B_Scene`에 XR Origin 세팅 + Quest 컨트롤러로 공 집어 던지기 동작 확인 |
| 다음 주 후반 | A의 `WallBlock` 받아서 데미지 전달 연동, 일반 공 프리팹 완성 |

---

## 🤝 다른 팀원에게 협의 요청

### A (성벽 담당)에게
1. **`WallBlock` 클래스에 `public void TakeDamage(float damage)` 메서드 만들어주세요**
   - 제 `Projectile.cs`에서 호출할 인터페이스
2. **성벽 블록에 `Rigidbody` 필수** — `AddExplosionForce`가 먹히려면 필요
3. **약점 블록 태그 이름 통일** — `WeakPoint`로 갈까요? (유도 미사일용)

### C (게임 매니저 담당)에게
1. **투사체 가능 횟수는 `GameManager`에서 관리**
   - 제가 호출할 메서드: `GameManager.Instance.UseProjectile()` 이런 식?
2. **가챠 매니저의 프리팹 배열에 제가 만든 공 프리팹들 등록**
   - 제가 완성한 프리팹을 `GachaManager`의 `GameObject[] projectiles`에 드래그 등록

### D (UI / 통합 담당)에게
1. **남은 투사체 개수를 UI에 표시** — 어떤 변수에서 가져올지 협의
2. **완성된 공 프리팹 전달 방식** — 깃허브 푸시로 충분한지?

---

## ⚠️ 리스크 / 막힌 점

- 본격 구현은 다음 주부터 시작 (이번 주는 환경 세팅 + 골격)
- Meta XR SDK는 라이선스 동의 필요해서 각자 따로 설치해야 함 (README Step 9 참고)
- 실기기 빌드 테스트는 D랑 같이 해야 할 듯
